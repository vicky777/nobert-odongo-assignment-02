#lang racket

;; Procedure called to prompt user for input
(define (input-prompt string)
  (newline)
  (display string))

;; Procedure to capture user input
(define (<<)
  (read))

(define (sum)
  (let ([a (<<)]
    [b (<<)])
    (+ a b)))
